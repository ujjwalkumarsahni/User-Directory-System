<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Users</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body class="bg-gray-100">
    <a href="/" class="text-2xl p-6 text-blue-800">Back to home</a>

    <div class="max-w-6xl mx-auto p-6">
        <h1 class="text-3xl font-semibold text-center mb-6">All Users</h1>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            <% if (users.length === 0) { %>
                <p class="text-center text-gray-600 text-xl col-span-full">No users found.</p>
            <% } else { %>
                <% users.forEach((user) => { %>
                    <div class="bg-white rounded-lg shadow-lg p-4">
                        <img src="<%= user.imageURL %>" alt="<%= user.name %>" class="w-full h-40 object-cover rounded-md mb-4">
                        <h2 class="text-xl font-semibold text-gray-800 mb-2"><%= user.name %></h2>
                        <p class="text-gray-600 text-sm mb-2">Email: <%= user.email %></p>
                        <div class="flex justify-between">
                            <a href="/edit/<%= user._id %>">
                                <button class="bg-yellow-500 text-white py-1 px-4 rounded-md hover:bg-yellow-600">Edit</button>
                            </a>
                            <a href="/delete/<%= user._id %>">
                                <button class="bg-red-500 text-white py-1 px-4 rounded-md hover:bg-red-600">Delete</button>
                            </a>
                        </div>
                    </div>
                <% }); %>
            <% } %>
        </div>
    </div>

</body>

</html>
